import * as types from '../types'

const initialState = {
    name: 'Lê Hùng'
}

const userReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default userReducer;